#!/usr/bin/env bash
set -e

if [[ -f "/home/skogix/harness/.venv/bin/activate" ]]; then
    source "/home/skogix/harness/.venv/bin/activate"
fi

python "/home/skogix/harness/argc/run-tool.py" "$(basename "$0")" "$@"
