# Moved

Skill content that used to live here has moved to
`skogharness/src/skogharness/skills/` — that's the new source of truth.
Use `skogharness install skills` to copy it into `.claude/skills/`.

Note: several of the moved skills reference a shared
`../../references/harness-patterns.md` file that does not exist anywhere
in this repo (pre-existing gap, not introduced by the move).
